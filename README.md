# SkyFitnessPro

Современное фитнес-приложение для домашних тренировок на базе Next.js с TypeScript.

## 🎯 Основной функционал

- ✅ Просмотр курсов тренировок
- ✅ Личный кабинет пользователя
- ✅ Отслеживание прогресса тренировок
- ✅ Просмотр видеозаписей упражнений
- ✅ Управление курсами и тренировками
- ✅ Система аутентификации (JWT)
- ✅ **Полностью адаптивный дизайн** (320px - 4K+)

## 🛠 Технологический стек

- **Next.js 14** - React фреймворк
- **TypeScript** - типизация
- **MongoDB + Mongoose** - база данных
- **JWT** - аутентификация
- **SWR** - управление состоянием сервера
- **React Hook Form + Zod** - управление формами и валидация

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 18.x или выше
- MongoDB (локальная установка или MongoDB Atlas)

### Установка

1. Клонируйте репозиторий

```bash
git clone <repository-url>
cd SkyFitnessPro
```

2. Установите зависимости

```bash
npm install
```

3. Создайте файл `.env.local` в корне проекта:

```env
MONGODB_URI=mongodb://localhost:27017/skyfitnesspro
JWT_SECRET=your-secret-key-here-change-in-production
NEXT_PUBLIC_API_URL=http://localhost:3000
```

4. Запустите MongoDB (если используете локальную установку)

5. Заполните базу данных тестовыми данными (опционально)

```bash
npm run seed
```

6. Запустите проект в режиме разработки

```bash
npm run dev
```

7. Откройте [http://localhost:3000](http://localhost:3000) в браузере

## 📱 Мобильная версия

Сайт **полностью адаптивен** и корректно отображается на всех устройствах!

### Поддерживаемые устройства:

- 📱 **Смартфоны** (320px - 767px)
  - iPhone (SE, 12, 13, 14, Pro Max)
  - Samsung Galaxy (S20, S21, S22)
  - Google Pixel
- 📱 **Планшеты** (768px - 1199px)
  - iPad, iPad Air, iPad Pro
  - Android планшеты
- 💻 **Desktop** (1200px+)
  - Любые размеры экранов

### Как протестировать:

1. Откройте `http://localhost:3000`
2. Нажмите `F12` (DevTools)
3. Нажмите `Ctrl+Shift+M` (режим устройства)
4. Выберите устройство (iPhone 12, iPad и т.д.)

📖 **Подробная документация:** см. файл `MOBILE_QUICKSTART.md`

## 📝 Доступные скрипты

| Команда              | Описание                        |
| -------------------- | ------------------------------- |
| `npm run dev`        | Запуск в режиме разработки      |
| `npm run build`      | Сборка для продакшена           |
| `npm run start`      | Запуск продакшен-сборки         |
| `npm run lint`       | Проверка кода линтером          |
| `npm run format`     | Форматирование кода Prettier    |
| `npm test`           | Запуск тестов (66 тестов)       |
| `npm run test:watch` | Запуск тестов в watch режиме    |
| `npm run seed`       | Заполнение БД тестовыми данными |

## 🧪 Тестирование

Проект покрыт unit-тестами с использованием Jest и React Testing Library.

```bash
npm test
```

### Покрытие тестами:

- ✅ Утилиты (`lib/utils.ts`) - 23 теста
- ✅ Константы (`lib/constants.ts`) - 10 тестов
- ✅ Хуки (`hooks/useModal.ts`) - 8 тестов
- ✅ Компоненты (AuthCard, CourseCard, ScrollToTop, Layout) - 25 тестов

**Итого: 66 тестов**

## 📁 Структура проекта

```
SkyFitnessPro/
├── components/         # React компоненты
├── lib/               # Утилиты и хелперы
├── models/            # Mongoose модели
├── pages/             # Next.js страницы и API routes
│   ├── api/          # API endpoints
│   └── ...           # Страницы приложения
├── public/           # Статические файлы
├── styles/           # CSS модули и глобальные стили
├── types/            # TypeScript типы и интерфейсы
└── ...
```

## 🔧 Конфигурация

### MongoDB

Для работы приложения требуется подключение к MongoDB. Вы можете использовать:

- Локальную установку MongoDB
- MongoDB Atlas (облачная база данных)

Строка подключения указывается в файле `.env.local`.

### Переменные окружения

Создайте файл `.env.local` со следующими переменными:

```env
MONGODB_URI=         # Строка подключения к MongoDB
JWT_SECRET=          # Секретный ключ для JWT токенов
NEXT_PUBLIC_API_URL= # URL вашего API (http://localhost:3000 для разработки)
```

## 📦 Развертывание

Проект настроен для развертывания на **Cloudflare Pages**.

### Шаги для деплоя:

1. Соберите проект:

```bash
npm run build
```

2. Следуйте инструкциям Cloudflare Pages для развертывания Next.js приложения

3. Настройте переменные окружения в панели управления Cloudflare Pages

## 🤝 Вклад в проект

Мы приветствуем вклад в проект! Пожалуйста:

1. Создайте форк проекта
2. Создайте ветку для вашей функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

## 📞 Контакты

Если у вас есть вопросы или предложения, пожалуйста, создайте issue в репозитории.
